/* 
document.getElementById();
document.querySelector();

document.getElementsByClassName();
document.getElementsByTagName();
document.querySelectorAll();




NB:
- Une Collection HTM n'est pas un array
- On ne peu pas utiliser la méthode forEach, for...of sur une collection HTMl
- On peut utiliser la boucle for sur une collection HTML
- on peut convertir une collection HTML en array avec Array.from()
- On ne peut applique le style, texteContent, innertext, innerHTM que sur un seule element.
- querySelectorAll() retournera une liste de noeuds(nodeLit)
- querySelectorAll() ne retourne pas une collection html
- on peut directment utilise: utilise les boucles for, for of et forEach
- Dans JS background-color 'secrit:  BackgroundColor, marginTop
- .childNodes: retourne une liste de noeuds incluant les sauts de lignes
- .children: retourne une collection html ne comprenant les elements html
-
 */
