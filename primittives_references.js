// 2 categories de donnees : Primitives et les references

// PRIMITIVES
// 1. String (chaînes de characters)
const name = "John Doe";
// 2. Number(Nombres)
const age = 45;
// 3. Boolean(Booléens)
const refuse = true;
// 4. Undefined(indéfinie)
let test;

// REFERENCES
// 1. Arrays(tableau)
const hobbies = ["movies", "music", "sport"];
// 2. objects
const address = {
  city: "Boston",
  state: "MaA",
  salary: 45690,
};
